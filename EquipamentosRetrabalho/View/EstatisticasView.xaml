<UserControl x:Class="EquipamentosRetrabalho.View.EstatisticasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:EquipamentosRetrabalho.View"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
             mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Margin="20">
            <TextBlock Text="{Binding TotalRetrabalhadosTexto}" FontSize="18" FontWeight="Bold" Margin="0,0,0,10"/>
            <TextBlock Text="{Binding QuantidadeMotoresTexto}" FontSize="16" Foreground="DarkBlue"/>
            <TextBlock Text="{Binding QuantidadeRedutoresTexto}" FontSize="16" Foreground="DarkRed" Margin="0,0,0,20"/>

            <TextBlock Text="Quantidade por equipamento:" FontWeight="Bold" />
            <ListBox ItemsSource="{Binding QuantidadePorTipo}" Margin="0,5,0,15" />

            <TextBlock Text="Quantidade por tipo de redutor:" FontWeight="Bold" />
            <ListBox ItemsSource="{Binding QuantidadePorTipoRedutor}" Margin="0,5,0,15" />

            <TextBlock Text="Gráfico de Motores e Redutores:" FontWeight="Bold" Margin="0,20,0,10"/>
            <lvc:CartesianChart Series="{Binding Series}"
                                XAxes="{Binding XAxes}"
                                YAxes="{Binding YAxes}"
                                Height="300"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>
